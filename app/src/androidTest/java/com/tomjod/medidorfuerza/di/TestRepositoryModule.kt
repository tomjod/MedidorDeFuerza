package com.tomjod.medidorfuerza.di

// This file was previously used to provide a Test Hilt module replacement.
// Currently the project binds the real `BleServiceManager` in production
// (see `RepositoryModule.kt`). If you want to provide a test-only
// binding for androidTest, add a proper TestInstallIn module here and
// ensure `com.google.dagger:hilt-android-testing` is available in
// `androidTestImplementation`.

// Intentionally left blank to avoid compilation issues while running
// static checks in the workspace.
